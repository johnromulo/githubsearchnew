import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  flex-direction: row;
  font-size: 25px;
  strong {
    font-weight: normal;
    color: #fff;

    span {
      font-weight: 200;
      font-style: italic;
      font-family: 'Raleway', sans-serif;
    }
  }
`;
